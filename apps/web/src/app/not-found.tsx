import { RouteMessage } from "@/components/ui/route-message";

export default function NotFound() {
  return <RouteMessage title="Page not found"><p>This address does not lead to a MealMind page. Find your way back to the collection.</p></RouteMessage>;
}
