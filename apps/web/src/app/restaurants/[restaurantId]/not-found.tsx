import { RouteMessage } from "@/components/ui/route-message";

export default function RestaurantNotFound() {
  return <RouteMessage title="Restaurant not found"><p>We could not find a restaurant at this address. Choose another from the collection.</p></RouteMessage>;
}
